Wire-compatible with 0.35.x: a new client streams from an old host and the other way round, so you can update one side at a time. On Windows the installer still updates the host and its display driver together.

- **Let a friend in from the internet, safely.** A device knocking from outside your network is marked, can't be approved by its name, and gets in only with a PIN you arm for that one device.
- **Each device gets its own display settings.** The TV keeps its screen for hours while the tablet's goes at once, and a phone can be capped below 4K.
- **A rebuilt web console.** A live map of your screens, five destinations, and it follows your desktop's theme.
- **iPhone and iPad stream to an external monitor** at the monitor's own size and refresh.
- **Android keeps a stream running in the background**, and a keyboard's Alt+Tab, Win chords and Korean and Japanese keys reach the host.
- **Apple devices heal a lost packet without a full refresh**, and a notched Mac fills the whole screen.
- **Android handhelds pace frames evenly**, and hosts show up on Wi-Fi that used to hide them.
- **Gamescope hosts stream to Linux clients again**, and Hyprland keeps your screen across a reconnect.

## Before you update

- **Windows 10 can't run the host.** The installer now refuses a host install below Windows 11 22H2 before it changes anything; the virtual display never started there. The client still installs on Windows 10.

## New

### Pairing and guests

- **Knocks from the internet are marked.** The console tags a pending device that knocked from outside your network "From the internet" and offers no Approve for it. **Arm PIN** opens a PIN for that one device; a PIN you armed for your own next device never admits it. Devices on your network and on Tailscale pair as before.
- **"Until they disconnect."** A new expiry for a paired device: a minute after its last session ends, its record is removed, so nothing is left to clean up. It is the default for a PIN armed for a device from the internet.
- **The Connect card's link works on a new device.** It now carries the host's address and fingerprint, so a friend's first connect is verified instead of blind.
- **The [Friends over the internet](https://docs.punktfunk.unom.io/docs/friends-over-the-internet) guide** covers port forwarding as the last resort, with the rules above.

### Displays

- **Each paired device can have its own display settings**, from its row on the console's Devices page. Choose how long its screen stays after it leaves, the largest size and refresh it gets, and what happens when its mode clashes with another device's. On Linux it can also choose whether your monitors go dark, and the scale its screen starts at. Anything left unset follows the host.
- **A capped device is told what it gets.** It keeps what fits and loses only the rest: a phone capped at 1440p60 that asks for 1080p at 240 Hz streams 1080p60.
- **Keep one monitor on while a device takes over the rest**, on KDE.

### Web console

- **Displays is a live map of your screens.** Drag a screen to arrange it, pick a preset right on the page, and one sentence says what happens when a device connects.
- **Five destinations:** Home, Devices, Displays, Library and Host, with the rest under Manage. A phone gets a bottom bar and a list, a tablet a rail of icons. A plugin's page is pinned from the Plugins page.
- **The console follows your desktop's theme** on GNOME, Plasma, COSMIC, Hyprland, sway and Windows. Settings → Appearance overrides it, and a chosen accent recolours the whole console.
- **The activity feed keeps the newest dozen events**, with a page of its own for the rest.
- **Automation suggests what the host knows:** events by name, devices, and games with their covers.

### iPhone, iPad and Mac

- **An external monitor gets the stream.** On a USB-C or AirPlay monitor the picture moves there at the monitor's size and top refresh, so a 120 Hz monitor gets 120 fps. Touch, the stats and the quick-action ring stay on the phone; unplugging hands the picture back.
- **L3 and R3 on the on-screen controller**, on iPhone, iPad and Android.
- **"This display" is your Mac panel's real size.** A scaled Mac used to stream the larger picture the system shrinks: 3420×2224 on a 2560×1664 MacBook Air.
- **Notched Macs.** Full screen fills the panel, camera housing included, and a new "This display (below the notch)" mode sits flush under it.

### Android

- **Keep streaming in the background.** Turn it on in settings and a glance at another app no longer ends the stream: the sound keeps playing, and the notification's End button stops it. Not on Android TV.
- **Every key reaches the host.** Turn on **Punktfunk keyboard shortcuts** under Android's Accessibility settings and Alt+Tab, Win chords and the Language key reach the host; without it, Alt+\` stands in for Alt+Tab. The Korean 한/영 and 한자 keys, the Japanese input keys and the Brazilian ABNT2 extras reach it too.
- **A mouse's Back button goes to the host**, and Ctrl+Alt+Shift+O opens the quick-action ring from a keyboard.
- **Steam Controller 2.** A wired SC2, a Puck or a DualSense keeps its USB permission across plugs, including on Android TV where the prompt often never appeared. An uncaptured SC2 drives the menus as a pad and names itself on the controller chip.
- **A hovering mouse moves the console's cursor.** A TV remote, arrow keys and Escape drive the quick-action ring.
- **A keyboard on a combined receiver types in the console**, and its Y and X keys open a tile's options.
- **The stats name Android's frame-rate cap.** When Android holds the app below the panel's rate, the first line says so and names the setting to check. The everyday tier shows only lost frames.

### Hyprland and Omarchy

- **Keep-alive on Hyprland.** A disconnect leaves the streamed screen up for the keep-alive window, and reconnecting returns to it instead of an empty one. When it does come down, its windows move to a physical monitor instead of disappearing.
- **A new Omarchy bar panel** with Session, Pairing, Devices and Display sections. Super+Space gains rows for Dedicated display or This screen, a Moonlight PIN, Unpair and GameStream.
- **Tailscale reaches an Omarchy host that joins a tailnet later.** Setup installs the firewall rule before the interface exists.

### Everywhere

- **The host names the screen it couldn't find.** A host set to stream a monitor it no longer has fails the session at once with that monitor's name, instead of a black screen for 15 seconds and a generic error. The console marks the missing screen.
- **Linux and Windows clients say when a pinned bitrate is too much for the device**, instead of stuttering silently.
- **Choose the console password during setup on Linux.** Keep the generated one, with the command that prints it, or type your own. On Windows, `punktfunk-host web password` in an elevated PowerShell prints it after a silent or winget install.
- **The console's sign-in says when a locked-out attempt can retry.**

## Improved

- **Recovery no longer feeds the overload it repairs.** A client that can't decode the stream no longer gets a full keyframe on a fixed beat; each one that does not heal doubles the wait, up to 3 s. Loss protection follows what the network loses, not frames a slow client dropped, so the host stops spending bandwidth on parity it can't use.
- **Logs tell a network drop from a stalled client.** The host logs what left its network card, the client logs how long nothing arrived versus how long it stopped reading, and Android logs every refresh-rate change of the panel.
- **The console's Logs tab shows host events** instead of a wall of TLS handshakes.
- **PyroWave on Android TV.** A device that can't decode it says "unsupported" and names the requirement, instead of offering a choice that did nothing.

## Fixed

- **iPhone, iPad, Apple TV and Mac:** a single lost packet no longer freezes the picture about 300 ms for a full keyframe; the repair heals it. On an iPad, a fast trackpad double-click no longer hides the cursor for half a second on every later click.
- **Windows clients on AMD graphics, such as the ROG Ally:** after a lost frame, HEVC no longer decodes smeared until the next keyframe.
- **Linux and Windows clients:** V-sync on no longer tears on a display that offers only immediate and FIFO presentation.
- **Android:** hosts appear on Wi-Fi networks that stop forwarding discovery to a power-saving phone, such as UniFi's default. A phone started with a controller attached asks for the network permission, so its host list is no longer empty. A stream no longer skips every other frame after a short slow patch or a refresh-rate switch. A paired host stays reachable after the TV moves between Wi-Fi and Ethernet. The TV stats overlay fits the screen.
- **Dual-boot hosts:** both OS installs of one box stay two hosts. Pairing one no longer retires the other, forgetting by address refuses to remove both, and the online dot checks which one answered. A machine that took a sleeping host's address no longer reads as that host, so Wake-on-LAN fires.
- **Linux host:** Steam, Lutris and other X11 apps launched from the library open instead of failing "Unable to open a connection to X". A second session on a GNOME or KDE host no longer stalls at start. A KDE host above 60 Hz keeps the refresh rate you asked for when KDE remembered an old layout. Super no longer sticks on Hyprland after a client loses a key release. The plugin runner can be switched back on after it was switched off, and an Ubuntu install no longer refuses every plugin. Windows-only display switches no longer show on a Linux host.
- **Gamescope hosts:** Linux clients get a picture again, HDR included. On NixOS a nested session finds Steam and its reaper.
- **SteamOS host:** a desktop with an NVIDIA card streams. A host built without an encoder no longer blanks a KDE desk eight times before giving up. A source build's update card reports the commit it built and offers an update only when there is one.
- **Setup:** Bazzite no longer waits forever on "Controller access".
- **Omarchy:** the menu's checked capture row follows a `PUNKTFUNK_CAPTURE_MONITOR` override.
- **Windows host:** a hung display driver no longer hangs every session while the host reports busy; the host reloads it. One tray icon per signed-in user again. Uninstalling the client removes its Start menu and desktop shortcuts.
- **Web console:** Firefox's saved password signs in, and a signed-in visit to the sign-in page goes on to where you were headed. Game sources carry their plugin's name, their settings open for a plugin that keeps them on its own page, a running source shows as running, and a refusal says why.

## Security

- **A device from the internet can't be admitted by its name.** The host itself refuses Approve for it; only a PIN armed for that device works.
- **The host checks that a first packet's sender can answer** before it spends a handshake on it, so a spoofed flood can't make it amplify.
- **The web console's dependencies moved past two Next.js remote code execution advisories** and two others.
- **A crafted sign-in link can no longer loop the console**, and the redirect check behind it now has a test.
- **A configured gamescope layer path is quoted**, so shell syntax in it can't run.

## Thanks

One person outside the team wrote code that ships in this release.

- **luxus** brought gamescope hosts back for Linux clients: the PipeWire format negotiation, HDR capture and the NixOS wrapper fixes. They made Hyprland keep a reconnecting client's screen and windows, rebuilt the Omarchy bar panel and its Super+Space host menu, and set up Tailscale's firewall rule ahead of the interface. They also narrowed which CI lanes a change wakes.

## For developers

Compat card: [CHANGELOG.md](https://git.unom.io/unom/punktfunk/src/tag/v0.36.0/CHANGELOG.md).
Deep dive: `git log v0.35.0..v0.36.0`
